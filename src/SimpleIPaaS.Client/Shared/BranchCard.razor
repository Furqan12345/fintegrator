@using SimpleIPaaS.Shared.Models

<MinimizableNodeCard Title="Branch" NodeColorClass="node-header-success">
    <p class="node-hint">
        Write a C# script that returns a boolean to route output paths.
    </p>
    <div class="node-field">
        <label class="node-label">Node Name</label>
        <input type="text" class="form-control" @bind="Step.NodeName" placeholder="e.g. CheckStatus" />
    </div>
    <button class="btn btn-block" @onclick="OpenEditor">
        @(string.IsNullOrWhiteSpace(Step.MappingCode) ? "Open Code Editor" : "Edit Branch Script")
    </button>
    @if (!string.IsNullOrWhiteSpace(Step.MappingCode))
    {
        <div class="node-code-preview">
            @Step.MappingCode.Split('\n').FirstOrDefault(l => !string.IsNullOrWhiteSpace(l) && !l.TrimStart().StartsWith("//"))
        </div>
    }
    <div class="branch-ports-info">
        <small class="text-muted">
            Connect Right port (True) or Bottom port (False) to route the execution.
        </small>
    </div>
</MinimizableNodeCard>

@code {
    [Parameter] public IntegrationStepDto Step { get; set; } = default!;
    [CascadingParameter] public SimpleIPaaS.Client.Pages.FlowDesigner Designer { get; set; } = default!;

    private void OpenEditor()
    {
        Designer?.OpenMapperModal(Step, "MappingCode");
    }
}
